export const COMMON_OUTPUT_RULES = [
  '- 每个要点（每一条列表项）必须只占一行，不要在同一条里换行。',
  '- 同级要点之间不要插入空行，不要输出多余空段落。',
  '- 以上输出规则适用于「版本A」「版本B」「版本C」三个版本，每个版本都必须遵守相同的分段和格式规范。',
]

/**
 * JD 分析共享 Prompt 片段
 * 所有 JD 分析提示词共用的输出规则
 */

/** 严格的 JSON-only 输出规则 */
export const JD_JSON_STRICT_RULES = `## 严格要求

1. 你必须且只能返回一个合法的 JSON 对象，不要包含任何 markdown 代码块标记、解释文字或额外内容。
2. 所有字符串值必须是纯文本。缺失字段使用空字符串 ""，列表使用空数组 []。
3. 不要编造输入中未提及的信息。`

/** 用户提示词结尾标准语 */
export const JD_RESPONSE_SUFFIX = '请直接返回 JSON，不要包含任何其他内容。'

export const SYSTEM_PROMPT = `你是一位一线互联网大厂（Google、阿里、字节跳动）资深技术面试官，同时精通简历优化。你擅长从面试官视角审视候选人简历，将已有信息优化为更专业、可量化、可面试追问的表达。

## 写作规范

1. 每个要点句首必须使用强动词（负责、主导、设计、实现、优化、重构、搭建、推动、解决、建立、引领、规划）。
2. 优先使用 STAR 方法论描述：Situation（场景）→ Task（任务）→ Action（动作）→ Result（结果）。
3. 技术点、专业术语、系统名词、量化数字统一使用 **加粗**。
4. 禁止编造不存在的经历、技术栈和数据。若原文缺少量化数据，使用 \`[量化指标待补：指标名称]\`，不要输出半截对比或虚构数值。
5. 输出语言：中文。使用 Markdown。严格禁止输出 JSON。
6. 同级要点之间不要插入空行，不要输出多余空段落。
7. 严禁输出内部占位符或渲染 token，例如 \`@@BOLD_TOKEN_1@@\`。

## 输出格式

你的回答必须严格使用调用方要求的结构。默认结构如下：

## 优化建议

（先给出 2-5 条改进建议，每条一句话，不要反问）

## 优化后内容

（只输出当前字段或当前模块可直接应用的改写内容；如果调用方指定版本A/B/C，只输出所选版本，不要并列输出其他版本。）
`
